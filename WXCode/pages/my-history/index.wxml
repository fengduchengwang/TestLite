<t-navbar title="浏览记录" left-arrow />
<view class="record-page">
  <view wx:if="{{cardInfo.length === 0}}" class="record-empty">暂无浏览记录</view>
  <view wx:else class="record-list">
    <card
      wx:for="{{cardInfo}}"
      wx:key="id"
      test-id="{{item.id}}"
      title="{{item.title}}"
      subtitle="{{item.subtitle}}"
      question-count="{{item.questionCount}}"
      duration="{{item.duration}}"
      url="{{item.url}}"
      link="{{item.link}}"
      tested="{{item.tested}}"
      followed="{{item.followed}}"
      action-mode="follow"
      tags="{{item.tags}}"
      bind:tested="onTested"
      bind:follow="onFollow"
      bind:unfollow="onUnfollow"
    />
  </view>
</view>
